{
  "name": "appointment-crm-starter",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm --prefix apps/api run dev\" \"npm --prefix apps/web run dev\"",
    "build": "npm --prefix apps/api run build && npm --prefix apps/web run build",
    "start": "concurrently \"npm --prefix apps/api start\" \"npm --prefix apps/web start\"",
    "postinstall": "npm --prefix apps/api install && npm --prefix apps/web install"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}